import React from "react";
import { Container, Typography, Box, Grid, duration } from "@mui/material";
import { grey } from "@mui/material/colors";
import Navbar from "../components/Navbar";

export default function HomePage() {
  //Sample data
  const shoeStats = {
    pairs: 2,
    totalMileage: 300,
    averageMileage: 150,
  };

  const lastRun = {
    date: "2024-10-20",
    distance: 10, //Km
    duration: "50 minutes",
  };

  return (
    
  );
};
